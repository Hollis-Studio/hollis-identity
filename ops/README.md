# Operations

ECS task definitions are now generated by Terraform in `../infrastructure`.
Use `terraform plan` from that directory to review the rendered service, task,
IAM, network, RDS, Secrets Manager, and WAF resources for the target AWS account.
